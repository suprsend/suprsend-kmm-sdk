import app.suprsend.android.user.Company;

CREATE TABLE UserModel (
    id TEXT NOT NULL,
    name TEXT NOT NULL,
    company TEXT as Company
);

insertItem:
INSERT OR FAIL INTO UserModel(id, name,company) VALUES (?, ?, ?);


selectAll:
SELECT *
FROM UserModel;